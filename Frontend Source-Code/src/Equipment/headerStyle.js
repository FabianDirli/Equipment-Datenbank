import { makeStyles } from "@material-ui/core";

const useStyle = makeStyles(() => ({
    headerElement: {
        minWidth: "180px",
        marginLeft: "50px"
    },
    button: {
        position: "absolute",
        right: "30px",
        top: "60px"
    }
}))

export default useStyle;